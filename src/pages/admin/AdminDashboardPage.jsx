import { Business, EventNote, Payments, People, Stadium } from '@mui/icons-material';
import { Skeleton } from '@mui/material';
import { useAdminDashboard } from '../../hooks/useAdmin';
import { formatCurrency } from '../../utils/formatters';
export default function AdminDashboardPage(){const {data,isLoading}=useAdminDashboard();const cards=[[People,'Customers',data?.customers],[Business,'Businesses',data?.businesses],[Stadium,'Venues',data?.venues],[EventNote,'Bookings',data?.bookings],[Payments,'Gross value',formatCurrency(data?.grossBookingValue||0,'LKR')]];return <><p className="eyebrow">Platform overview</p><h1 className="mt-2 text-3xl font-black">Super admin dashboard</h1>{data?.pendingVenues>0&&<div className="mt-6 rounded-2xl border border-amber-300 bg-amber-50 p-4 font-bold text-amber-900">{data.pendingVenues} venue{data.pendingVenues===1?' is':'s are'} waiting for approval.</div>}<div className="mt-7 grid gap-4 sm:grid-cols-2 xl:grid-cols-5">{cards.map(([Icon,label,value])=><div key={label} className="surface-card p-5"><Icon className="text-lime-600"/><span className="mt-5 block text-sm text-muted">{label}</span>{isLoading?<Skeleton/>:<strong className="mt-1 block text-2xl">{value}</strong>}</div>)}</div></>}
